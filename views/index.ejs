<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Riddle Time!</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link href="/styles/styles.css" rel="stylesheet" type="text/css">
</head>
<body>
    <div class="container text-center ">
<!-- if there is data from the API -->    
        <% if (locals.riddle) {%>
            <h2 class="text-body-emphasis">Riddle</h2>
            <p class="text-muted"> <%= riddle %> </p>

        <% if (locals.answer) { %>
            <h2 class="text-body-emphasis">Answer</h2>
            <p class="text-muted"> <%= answer %> </p>
        <% } %>
            <div class="d-inline-flex gap-2 mb-5">
                <% if (!locals.answer) { %>
                    <form action="/answer" name="showAnswer" method="POST">
                        <button class="d-inline-flex align-items-center btn btn-warning btn-sm px-4 rounded-pill" type="submit">Answer 🕵🏻‍♀️</button>
                    </form>
                <% } %>
                <form action="/" name="generateRiddle" method="POST">
                    <button class="btn btn-primary btn-sm px-4 rounded-pill" type="submit">Give me another! 🔎</button> 
                </form>
            </div>
            
<!-- if there is an error  -->
            <% } else if (locals.error) { %>
                <h2><%= error %></h2>


<!-- if there is no data yet -->            
        <% } else { %>
            <h1 class="text-body-emphasis">Riddle Time!</h1>
            <div class="d-inline-flex gap-2 mb-5">
                <form action="/" name="generateRiddle" method="POST">
                    <button class="btn btn-success btn-sm px-4 rounded-pill" type="submit">Lets do this 🔎</button> 
                </form>
            </div>
        <% } %>


<!-- footer -->
        <p class="small"> Copyright © <%- currentYear %> </p>

  </div>


<!-- bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>


